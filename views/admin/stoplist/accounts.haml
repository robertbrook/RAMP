%p You are an admin user on RAMP. You can add an account or an individual photo to the stop list.

%table.detail
  %caption Stopped Flickr accounts
  %thead
    %tr
      %th Flickr Account
      %th Action
  %tbody
  - i = 0
  - @stoplist_users.each do |stopped|
    %tr
      %td
        %a{:href => "http://flickr.com/photos/#{stopped}"}= @stoplist_usernames[i]
      %td
        &lt;&lt;link goes here&gt;&gt;
    - i += 1

%br