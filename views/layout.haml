%html
  %head
    %title Recognise An MP?
    %link{:type => "text/css", :rel => "stylesheet", :href => "/stylesheets/styles.css"}
  %body
    %h1 Recognise An MP?
    %p #{session[:attempts]} photos, #{session[:passes]} passes, #{session[:correct]} correct
    = yield
    - if request.path_info != "/about"
      %p <a href="about">More about this site</a>.    
      