.vcard
  .vcard_text
    .vcard_name
      = "#{@mp['name']}"
    .vcard_constituency
      = "MP for #{@mp['constituency']}"
    .vcard_party
      = "#{@mp['party']}"
  .vcard_photo
    %img{:src => @mp['twfy_photo']}